{
  "name": "mise-dotfiles",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "oxfmt --check . && oxlint --no-error-on-unmatched-pattern && markdownlint-cli2 && textlint \"**/*.md\""
  },
  "devDependencies": {
    "@types/bun": "latest",
    "markdownlint-cli2": "^0.23.2",
    "oxfmt": "0.64.0",
    "oxlint": "^1.79.0",
    "textlint": "^15.8.0",
    "textlint-rule-preset-ja-technical-writing": "^12.0.2",
    "typescript": "^7.0.2"
  }
}
